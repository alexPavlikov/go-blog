-----------ВАЖНО--------------
Если добавляешь человека в друзья выполнить два запроса, внесение его к себе в список друзей и тебя в его список друзей
тоже самое с подписчиками сообщества, если человек создал группу он будет первым подписчиком


ПЕРЕСТАЛО РАБОТАТЬ ОБНОВЛЕНИЕ просмотров

ошибка в консоли при выводе комментариев...

решать вопрос с мессенджером также при загрузке страницы опускаться в самый низ bara

не добавляет просмотры на странице сообщество и лайки кидаю на новости !

в таблице communities попробовать создать ключ Id---------

везде сделать автоинкремент---------------------

решать вопрос с лайками, чтобы можно было лайкнуть только один раз и на второй удалить лайк

При частом нажатии лайка в Новостях происходит дичь с запросом

Доделать смену аватара - можно лучше но пока как есть

удаление репостов плохо работает


if newPass != "" {
					_, err = database.UpdateUserByColumn("Password", newPass, login, oldPass)
					if err != nil {
						fmt.Println(err.Error())
					}
				} else if newName != "" {
					_, err = database.UpdateUserByColumn("Name", newName, login, oldPass)
					if err != nil {
						fmt.Println(err.Error())
					}
				} else if date != "" {
					_, err = database.UpdateUserByColumn("Birthdate", date, login, oldPass)
					// сделать проверку чтобы нельзя вписать дату больше сегодняшней
					if err != nil {
						fmt.Println(err.Error())
					}
				}


val, err := net.InterfaceAddrs()
	if err != nil {
		fmt.Println(err.Error())
	}
	fmt.Println(val)
