-----------ВАЖНО--------------
Если добавляешь человека в друзья выполнить два запроса, внесение его к себе в список друзей и тебя в его список друзей
тоже самое с подписчиками сообщества, если человек создал группу он будет первым подписчиком


ошибка в консоли при выводе комментариев...

SELECT "Communities".* //тупо работает когда все сообщества в подписках присутствуют у пользователя
	FROM "Communities"
	JOIN "Subscribers" ON "Subscribers"."Communities" is distinct from "Communities"."Name"
	WHERE "Subscribers"."User"='a.pavlikov2002@gmail.com';

решать вопрос с мессенджером 






			msg := models.Message{User: usrMesg, Message: message, Status: ok}
			// var data []models.Message
			// data = append(data, msg)

			f.Write([]byte("[\n"))
			jsn, err = json.MarshalIndent(msg, "", "\t")
			if err != nil {
				fmt.Println("Error - message() json.MarshalIndent()")
			}
			f.Write(jsn)
			f.Write([]byte(",\n"))
			f.Write([]byte("]\n"))
